sudo nano /etc/apache2/sites-enabled/001-cloud9.conf
http://phpqrcode.sourceforge.net/examples/index.php

mysql-ctl cli
use c9;
select @@hostname;
exit

modified setasign/fpdi_bridge (if ! 'TCPDF' extend tFPDF instead of FPDF).

copied font

pdf info fpdf

git clone https://github.com/gstankov/numerate
numerate/composer install
npm install
gulp